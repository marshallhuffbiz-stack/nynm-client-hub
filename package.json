{
  "name": "client-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NYNM Client Hub — client request portal + internal Request Desk, modeled on lead-responder.",
  "scripts": {
    "test": "node --test",
    "mock": "node mock-server/server.mjs",
    "seed": "node scripts/seed.mjs",
    "dev": "node scripts/dev.mjs"
  }
}
