.auto/
node_modules/
.DS_Store
data/*.json
worker/config.json
*.local.json

# auto-improve working dir
.auto-improve/

# live worker runtime artifacts — client data, never publish (GH Pages serves this repo)
worker/.state.json
worker/drain-jobs.json
worker/logs/
worker/out/
